# ===================================
#  Column Definitions
# ===================================

columns:

    id:
        label: crytofy.user::lang.user.id
        invisible: true

    username:
        label: crytofy.user::lang.user.username
        searchable: true
        invisible: true

    name:
        label: crytofy.user::lang.user.name
        searchable: true

    surname:
        label: crytofy.user::lang.user.surname
        searchable: true
        invisible: true

    email:
        label: crytofy.user::lang.user.email
        searchable: true

    created_at:
        label: crytofy.user::lang.user.created_at
        type: timetense

    last_seen:
        label: crytofy.user::lang.user.last_seen
        type: timetense

    is_guest:
        label: crytofy.user::lang.user.is_guest
        type: switch
        invisible: true
